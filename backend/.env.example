# Backend server
PORT=4000

# Elasticsearch (docker)
ES_NODE=http://localhost:9200

# Qdrant vector DB (docker)
QDRANT_URL=http://localhost:6333

# OpenRouter (DO NOT paste your private key here in public repos)
# Put your key in backend/.env (not in README)
OPENROUTER_API_KEY=
OPENROUTER_MODEL=mistralai/mistral-small-24b-instruct-2501:free
OPENROUTER_EMBED_MODEL=deepseek/deepseek-embed

# Google Gemini API (FREE)
GEMINI_API_KEY =

# Models
GEMINI_MODEL=gemini-1.5-flash-latest
GEMINI_EMBED_MODEL=text-embedding-004

# Slack incoming webhook (optional)
SLACK_WEBHOOK_URL=

# External webhook (for testing) - webhook.site URL
EXTERNAL_WEBHOOK=

# Two IMAP accounts JSON string (escape quotes if adding inline).
# Example value (single-line JSON). For Gmail use app passwords or OAuth.
IMAP_ACCOUNTS_JSON=[{"host":"mailslurp.click","port":1143,"secure":false,"user":"","password":""},{"host":"mailslurp.click","port":1143,"secure":false,"user":"","password":"F"}]
